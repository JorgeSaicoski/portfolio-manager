services:
  # Database
  portfolio-postgres:
    image: postgres:16-alpine
    container_name: portfolio-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-portfolio_db}
      POSTGRES_USER: ${POSTGRES_USER:-portfolio_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-portfolio_pass}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro,z
    networks:
      - portfolio-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-portfolio_user} -d ${POSTGRES_DB:-portfolio_db} && pg_isready -U ${POSTGRES_USER:-portfolio_user} -d authentik"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis for Authentik
  portfolio-redis:
    image: redis:7-alpine
    container_name: portfolio-redis
    restart: unless-stopped
    command: --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    networks:
      - portfolio-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Authentik Server
  portfolio-authentik-server:
    image: ghcr.io/goauthentik/server:2024.2.3
    container_name: portfolio-authentik-server
    restart: unless-stopped
    command: server
    ports:
      - "${AUTHENTIK_PORT:-9000}:9000"
      - "${AUTHENTIK_HTTPS_PORT:-9443}:9443"
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-change-me-to-a-random-string}
      AUTHENTIK_REDIS__HOST: portfolio-redis
      AUTHENTIK_POSTGRESQL__HOST: portfolio-postgres
      AUTHENTIK_POSTGRESQL__NAME: ${AUTHENTIK_DB_NAME:-authentik}
      AUTHENTIK_POSTGRESQL__USER: ${POSTGRES_USER:-portfolio_user}
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD:-portfolio_pass}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_LOG_LEVEL: ${AUTHENTIK_LOG_LEVEL:-info}
    volumes:
      - authentik_media:/media
      - authentik_templates:/templates
    depends_on:
      portfolio-postgres:
        condition: service_healthy
      portfolio-redis:
        condition: service_healthy
    networks:
      - portfolio-network
    healthcheck:
      test: ["CMD-SHELL", "python3 -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:9000/-/health/live/\")' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

  # Authentik Worker
  portfolio-authentik-worker:
    image: ghcr.io/goauthentik/server:2024.2.3
    container_name: portfolio-authentik-worker
    restart: unless-stopped
    command: worker
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-change-me-to-a-random-string}
      AUTHENTIK_REDIS__HOST: portfolio-redis
      AUTHENTIK_POSTGRESQL__HOST: portfolio-postgres
      AUTHENTIK_POSTGRESQL__NAME: ${AUTHENTIK_DB_NAME:-authentik}
      AUTHENTIK_POSTGRESQL__USER: ${POSTGRES_USER:-portfolio_user}
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD:-portfolio_pass}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_LOG_LEVEL: ${AUTHENTIK_LOG_LEVEL:-info}
    volumes:
      - authentik_media:/media
      - authentik_templates:/templates
    depends_on:
      portfolio-postgres:
        condition: service_healthy
      portfolio-redis:
        condition: service_healthy
    networks:
      - portfolio-network


  # Backend API Service
  portfolio-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: portfolio-backend
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - portfolio-audit:/app/audit
      - portfolio-uploads:/app/uploads
    environment:
      - PORT=${PORT:-8000}
      - GIN_MODE=${GIN_MODE:-release}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - DB_HOST=portfolio-postgres
      - DB_PORT=${DB_PORT:-5432}
      - DB_NAME=${POSTGRES_DB:-portfolio_db}
      - DB_USER=${POSTGRES_USER:-portfolio_user}
      - DB_PASSWORD=${POSTGRES_PASSWORD:-portfolio_pass}
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      - DB_TIMEZONE=${DB_TIMEZONE:-UTC}
      - AUTHENTIK_URL=http://portfolio-authentik-server:9000
      - AUTHENTIK_ISSUER=${AUTHENTIK_ISSUER:-http://localhost:9000/application/o/portfolio-manager/}
      - PROMETHEUS_AUTH_USER=${PROMETHEUS_AUTH_USER:-admin}
      - PROMETHEUS_AUTH_PASSWORD=${PROMETHEUS_AUTH_PASSWORD:-changeme-secure-password}
    depends_on:
      portfolio-postgres:
        condition: service_healthy
      portfolio-authentik-server:
        condition: service_healthy
    networks:
      - portfolio-network
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:8000/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend
  portfolio-frontend:  
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: portfolio-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - VITE_API_URL=${VITE_API_URL:-http://localhost:8000/api}
      - VITE_AUTHENTIK_URL=${VITE_AUTHENTIK_URL:-http://localhost:9000}
      - VITE_AUTHENTIK_CLIENT_ID=${AUTHENTIK_CLIENT_ID}
      - VITE_AUTHENTIK_REDIRECT_URI=${VITE_AUTHENTIK_REDIRECT_URI:-http://localhost:3000/auth/callback}
      - VITE_AUTHENTIK_ISSUER=${AUTHENTIK_ISSUER:-http://localhost:9000/application/o/portfolio-manager/}
    depends_on:
      - portfolio-authentik-server
      - portfolio-backend
    networks:
      - portfolio-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Prometheus - Monitoring Profile
  prometheus:
    image: prom/prometheus:latest
    container_name: portfolio-prometheus
    restart: unless-stopped
    profiles:
      - monitoring
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
      - "--storage.tsdb.retention.time=200h"
      - "--web.enable-lifecycle"
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:Z
      - ./monitoring/prometheus/rules:/etc/prometheus/rules:ro,Z
      - prometheus_data:/prometheus
    networks:
      - portfolio-network
    # user: "65534:65534"  # Removed to allow reading mounted config files

  # PostgreSQL Exporter - Monitoring Profile
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: portfolio-postgres-exporter
    restart: unless-stopped
    profiles:
      - monitoring
    environment:
      DATA_SOURCE_NAME: "postgresql://${POSTGRES_USER:-portfolio_user}:${POSTGRES_PASSWORD:-portfolio_pass}@portfolio-postgres:5432/${POSTGRES_DB:-portfolio_db}?sslmode=disable"
      PG_EXPORTER_EXTEND_QUERY_PATH: "/etc/postgres-exporter/queries.yaml"
    ports:
      - "9187:9187"
    volumes:
      - ./monitoring/postgres-exporter/queries.yaml:/etc/postgres-exporter/queries.yaml:ro,Z
    networks:
      - portfolio-network
    depends_on:
      - portfolio-postgres

  # Grafana - Monitoring Profile
  grafana:
    image: grafana/grafana:11.3.0
    container_name: portfolio-grafana
    restart: unless-stopped
    profiles:
      - monitoring
    environment:
      # Basic admin (fallback)
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false

      # OAuth2 with Authentik
      - GF_AUTH_GENERIC_OAUTH_ENABLED=true
      - GF_AUTH_GENERIC_OAUTH_NAME=Authentik
      - GF_AUTH_GENERIC_OAUTH_CLIENT_ID=${GRAFANA_OAUTH_CLIENT_ID}
      - GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET=${GRAFANA_OAUTH_CLIENT_SECRET}
      - GF_AUTH_GENERIC_OAUTH_SCOPES=openid profile email groups
      - GF_AUTH_GENERIC_OAUTH_AUTH_URL=http://portfolio-authentik-server:9000/application/o/authorize/
      - GF_AUTH_GENERIC_OAUTH_TOKEN_URL=http://portfolio-authentik-server:9000/application/o/token/
      - GF_AUTH_GENERIC_OAUTH_API_URL=http://portfolio-authentik-server:9000/application/o/userinfo/
      - GF_AUTH_SIGNOUT_REDIRECT_URL=http://localhost:9000/application/o/grafana/end-session/
      - GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH=contains(groups[*], 'Grafana Admins') && 'Admin' || contains(groups[*], 'Grafana Editors') && 'Editor' || 'Viewer'
      - GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP=true
      - GF_AUTH_GENERIC_OAUTH_AUTO_LOGIN=false

      # Server settings
      - GF_SERVER_ROOT_URL=http://localhost:3001
      - GF_SERVER_SERVE_FROM_SUB_PATH=false
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro,Z
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro,Z
    networks:
      - portfolio-network
    depends_on:
      - prometheus
      - portfolio-authentik-server

  # Adminer - Database UI (Optional Profile)
  adminer:
    image: adminer:latest
    container_name: portfolio-adminer
    restart: unless-stopped
    profiles:
      - db-ui
    environment:
      - ADMINER_DEFAULT_SERVER=portfolio-postgres
      - ADMINER_DESIGN=${ADMINER_DESIGN:-nette}
    ports:
      - "${ADMINER_PORT:-8080}:8080"
    networks:
      - portfolio-network
    depends_on:
      - portfolio-postgres

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  authentik_media:
    driver: local
  authentik_templates:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  portfolio-audit:
    driver: local
  portfolio-uploads:
    driver: local

networks:
  portfolio-network:
    driver: bridge
