# Test Environment Configuration
# This file uses the shared dev database from the Podman compose setup
# Tests clean up after themselves, so it's safe to share the DB in dev

# =====Database Configuration (Shared with Dev) =====
# These values should match the defaults in your compose configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio_test_db
DB_USER=portfolio_user
DB_PASSWORD=portfolio_pass
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# ===== Test Server Configuration =====
PORT=8888
JWT_SECRET=test-jwt-secret-key-for-testing-only

# ===== Testing Mode =====
# When TESTING_MODE=true, auth middleware bypasses the auth service
# and uses a test user (ID: 123, username: testuser)
TESTING_MODE=true

# ===== Auth Service (not used in testing mode) =====
AUTH_SERVICE_URL=http://localhost:8080
